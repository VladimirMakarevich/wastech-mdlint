# P13.01 · Glob semantics: negation, anchoring, and both documented

> Phase: [P13 — Correctness](index.md) · Roadmap: [v2 Index](../index.md) · Size **M** · Status **Done**. Backlog: [W-01](../remediation-backlog-2026-08-05.md) (Blocker), [W-03](../remediation-backlog-2026-08-05.md) (Medium). Sources: audit F1 (HIGH, reproduced 8 ways, re-reproduced independently), F25 (LOW); field F-14, F-07 (major). Depends on [P12](../P12-consistency/index.md). Blocks [P13.02](02-default-exclude.md), [P13.04](04-rule-option-defaults.md).

## Goal

Make a glob in config mean one predictable thing in both of its surprising directions: a `!` entry must subtract from the set or be rejected, and the anchoring rule that decides whether a pattern is root-anchored or depth-agnostic must be stated where a user writes config. These ship together because the documentation cannot be written until the negation decision is made.

## Problem

**Negation (W-01).** `matchesConfigGlob` is the single matcher behind every glob surface in the product, and it calls `micromatch.isMatch` with the whole pattern array ([`packages/core/src/discovery/globs.ts`](../../../packages/core/src/discovery/globs.ts) around `:37`). That call is a first-truthy OR across the list, and a `!`-prefixed entry compiles to an _inverting_ matcher — so `["docs/public/**", "!docs/private/**"]` reads as "under `docs/public` **or** not under `docs/private`", true for almost every path in a repository. A second failure mode sits in the same helper: `normalizeConfigGlob` rewrites a slash-free pattern to a depth-agnostic form (`:14`), so a bare `!keep.md` becomes a literal filename pattern and is a silent no-op. Nothing validates or rejects a leading `!`.

**The repository already diagnosed this and fixed it 200 lines away.** [`workspace-packages.ts`](../../../packages/core/src/discovery/workspace-packages.ts) states in prose at `:249` that an ordered negation "never actually excludes anything" with `isMatch`, uses the correct list form at `:254`, records the semantics in an ambient declaration at [`types/micromatch.d.ts`](../../../packages/core/src/types/micromatch.d.ts) `:8`, and pins it with two tests. The load-bearing matcher was never converted. The dedicated test file for the shared matcher has no ordered-negation case at all — its only `!` is an extglob syntax check in `packages/core/test/rule-utils.test.ts`.

**Blast radius.** Grepping `matchesConfigGlob|matchesFileScope` across `packages/core/src` gives every affected surface: top-level `include`/`exclude`, directory pruning, every rule's `files`/`exclude` through the shared scope helper, `LLM-001.entrypoints`, `SIZE-001.overrides.pattern`, `CTX-003.glossary`, `REF-006`, `GRP-002.entryPoints`, `GRP-003.chain[].files`, `STR-001` entries, REF-001/003 target `exclude`, and `SEC-001`'s inferred scope.

**Anchoring (W-03).** A pattern containing `/` is returned untouched (root-anchored); a pattern without `/` gets a `**/` prefix (any depth). Internally consistent, and documented at exactly one place — `docs/guide/rules/STR-001.md:25` — and nowhere in `docs/guide/configuration.md`, `docs/guide/config-reference.md`, or `README.md`. Both directions bite: `exclude: ["node_modules/**"]` prunes only the root copy and silently under-excludes a monorepo, which is exactly what `README.md:127` tells a user to copy; and `include: ["*.md"]` recurses into the whole tree, the opposite of shell, gitignore and tsconfig. `init` emits `"./*.{md,mdx}"` because the `./` is load-bearing, and nothing says so.

The field test measured the anchoring half on a real monorepo: no config / the README example / an any-depth prefix gave 3 / 2 / 1 files on the same three-file fixture.

## Deliverables / steps

1. **Decide the negation direction — it is a behavior change either way, so decide it deliberately and record the choice.**
   - **(A) Deliver negation:** route `matchesConfigGlob` through the list form already used at `workspace-packages.ts:254`. Negation becomes a feature; any corpus relying on today's accidental widening shrinks.
   - **(B) Reject it loudly:** reject a leading `!` during config validation, with a diagnostic that names the pattern and the key. Smaller behavioral change, turns a silent wrong answer into a loud one, does not deliver the feature. **If (B) is chosen, the diagnostic is a config diagnostic** — [P13.06](06-config-diagnostics.md) owns the path notation and the "name the key and the allowed values" shape for that validator, so emit the new message through whatever it settles rather than inventing a second format. The two tasks are otherwise independent; this is the one place they touch.
   - Either way, normalize a leading `!` **before** the depth-agnostic prefix at `globs.ts:14`, or a bare `!keep.md` stays a silent no-op under (A) and slips past validation under (B).
2. **Close it in one place.** The fix belongs in [`packages/core/src/discovery/globs.ts`](../../../packages/core/src/discovery/globs.ts), not per-surface — the whole point of the shared matcher is that every surface inherits it.
3. **Add the ordered-negation case to `packages/core/test/rule-utils.test.ts`**, which is where its absence let this ship. Cover the three reproduced shapes: negated `include`, negated `exclude`, and a negated rule-level `files`.
4. **Document the anchoring rule where config is written:** `docs/guide/configuration.md` and `docs/guide/config-reference.md`. State both directions and the `./` anchoring prefix.
5. **Fix `README.md:127`** to the any-depth form. `README.md:80` and [`discovery/config-writer.ts`](../../../packages/core/src/discovery/config-writer.ts) `:134` are already correct, so only the hand-copy example is wrong.
6. **Record the outcome.** If (B), the anchoring documentation says negation is rejected and why. If (A), it documents ordered evaluation. Either way [`glossary.md`](../glossary.md)'s **File scope** entry — which currently describes glob semantics as "picomatch with `{ dot: true }`" and says nothing about ordering — gains the rule.

## Out of scope

Changing what any shipped default `exclude` contains — that is [P13.02](02-default-exclude.md), which depends on this task's decision. Adding a warning for a slash-free directory-ish pattern that was probably meant to be anchored: the backlog notes it overlaps direction (B) above, so decide it here rather than twice, but implement it only if (B) is chosen.

## Exit criteria

- [x] `include: ["docs/**", "!docs/private/**"]` yields only `docs/public` — or is rejected with a diagnostic naming the pattern.
- [x] `exclude: ["docs/private/**", "!docs/private/keepme.md"]` no longer empties the corpus and no longer exits `0` on a repository with findings.
- [x] A rule-level negated `files` no longer pulls in a third file.
- [x] A slash-free `!a.md` is not a silent no-op.
- [x] `packages/core/test/rule-utils.test.ts` carries an ordered-negation case that fails before the fix.
- [x] If (B), the rejection diagnostic uses [P13.06](06-config-diagnostics.md)'s notation and message shape rather than a second format. — n/a, (A) was chosen.
- [x] Each of the four shapes in the field test's anchoring table (`NOTE.md`, `*.md`, `./NOTE.md`, `node_modules/**`) has a documented, predictable answer in `configuration.md` and `config-reference.md`.
- [x] The `README.md` example prunes a nested `node_modules`.
- [x] The glossary's **File scope** entry states the anchoring rule and the negation decision.
- [x] Gates green.

## Implementation notes

- **Direction (A): negation is delivered.** `matchesConfigGlob` now matches a one-item list (`micromatch([path], patterns, { dot: true }).length > 0`), the call shape [`workspace-packages.ts`](../../../packages/core/src/discovery/workspace-packages.ts) `:254` already used for the same reason. (B) was rejected on this task's own constraint: validation is per-schema, so rejecting a leading `!` means touching top-level `include`/`exclude` in `config-schema.ts`, `fileScopeShape`, and the pattern options of LLM-001/SIZE-001/CTX-003/REF-006/GRP-002/GRP-003/STR-001/REF-001/003/SEC-001 — the per-surface glob handling the task forbids — unless a new shared glob schema is threaded through all of them, which moves the generated `packages/cli/schema.json`. (A) is one file every surface inherits, needs no new diagnostic (so the one coupling to [P13.06](06-config-diagnostics.md) disappears), and nothing in the repository used a negated config glob. The optional slash-free-directory warning is (B)-only and was therefore not implemented.
- **Why the blast radius is provably tight.** In `micromatch(list, patterns)` (`node_modules/micromatch/index.js:32-82`), a list with no negated entry leaves `negatives === 0`, so the result is `[...keep]` — exactly "matched at least one pattern", byte-identical to the old `isMatch` OR. Empty `patterns` still yields `[]`. Behavior therefore changes **only** for lists containing a negation. The one-item list also keeps the semantics per-path: micromatch keys `keep`/`omit` by the matcher's `output`, which `picomatch/lib/picomatch.js:126-131` derives from the _input_ under options shared by every pattern, so the sets line up identically on Windows, macOS, and Linux.
- **Normalization now splits the negation before applying the depth-agnostic prefix**, so `!keep.md` becomes `!**/keep.md` rather than the literal-filename `**/!keep.md` that was a silent no-op. Three sub-rules, each pinned by a test: peeling **stops at `!(`**, which picomatch reads as a negated extglob rather than a negation (`picomatch/lib/parse.js` — the `!` branch opens an extglob when the next character is `(` and only calls `negate()` otherwise), so the working `**/!(x).md` is not rewritten into `!**/(x).md`; the **whole** run of `!` is peeled, so an even count cancels in picomatch's own `negate()` exactly as it already did in the slash-containing branch that passes through untouched; and a pattern that is nothing but `!` reports no negation, keeping `**/!` matching a file named `!`.
- **Two plan assumptions corrected against the installed sources.** (1) No `./`-stripping code was needed: picomatch strips a leading `./` relative to the start it advances past for a negation (`parse.js:950`, `prev.type === 'dot' && state.index === state.start + 1`), so `!./docs/**` already anchors like `!docs/**`. Pinned by a test rather than coded around. (2) `\!keep.md` is not the escape hatch for a filename starting with `!` and never was — `normalizePathValue` rewrites every `\` to `/` for Windows separators, so it becomes `/!keep.md` and matches nothing. The working form is the bracket class `[!]keep.md`, which is also what [`escapeGlobPath`](../../../packages/core/src/discovery/repo-scan.ts) `:81` emits and why it emits it. Both directions are now tested.
- **A bos `!(` with a slash is subtractive, and that is documented rather than changed.** micromatch's negated check is `state.negated || state.negatedExtglob` (`index.js:50`), and `negatedExtglob` is set when the extglob opens at `bos` (`parse.js:564`). So `!(docs)/**` passes through normalization untouched and is treated as a negation: `["docs/**", "!(docs)/**"]` selects nothing. The slash-free `!(draft).md` gets the `**/` prefix, which moves the `!` off `bos`, so it stays the plain extglob it has always been. The user-facing guidance is to write `!docs/**`.
- **Corpus-level limitation, accepted not fixed.** A negated `exclude` cannot re-include a file under a directory an earlier positive entry excludes: `shouldPruneDirectory` ([`load-documents.ts`](../../../packages/core/src/markdown/load-documents.ts) `:38-44`) decides per directory before descending, so nothing inside is offered to the file-level filter. Honoring it means walking every excluded tree — the cost `exclude` exists to avoid, on the same trees [P13.02](02-default-exclude.md) is about to prune by default. Row in the [accepted behaviors register](../accepted-behaviors.md), stated for users under [glob semantics](../../guide/configuration.md#glob-semantics). `shouldPruneDirectory` itself is unchanged.
- **Verified red before the fix.** Every negation case in `packages/core/test/rule-utils.test.ts` fails against the pre-change matcher — including the `!!` one, since the old normalization returned `**/!!keep.md` — while the anchoring, extglob, bracket-class, and empty-list pins stay green; the corpus case in `load-documents.test.ts` and the exit-code case in `packages/cli/test/lint.e2e.test.ts` were each confirmed red the same way. The CLI case is the user-visible half of the blocker: the old behavior emptied the corpus, printed "No problems found." and exited `0` on a repository with a REF-001 finding.
- **Nothing generated moved.** No schema `.describe()`, rule metadata, or config key changed, so `packages/cli/schema.json`, the generated README tables, and `docs-sync`/`schema-generation` are byte-identical. No new runtime dependency, no signature change to the two exported helpers, and no `@boundary-guard` tag — this adds no rule or assert kind and touches none of the four categories.
- **Anchoring documentation is now canonical in one place**, [`docs/guide/configuration.md`](../../guide/configuration.md#glob-semantics), with [`config-reference.md`](../../guide/config-reference.md), [`README.md`](../../../README.md#config), the glossary, and [STR-001](../../guide/rules/STR-001.md) — until now its only home — pointing at it. The hand-copied root-anchored `exclude` examples in `README.md`, `configuration.md`, `config-reference.md`, and [`use-cases/link-and-image-health.md`](../../guide/use-cases/link-and-image-health.md) are `**/`-prefixed, matching what `init` already writes. Deliberately left alone: [requirements/01-configuration.md](../requirements/01-configuration.md) `:28` carries the same snippet as an illustration of C1's _shape_, not a copy-paste starter, and C1's "include AND NOT exclude" wording stays true under ordered evaluation. What any default `exclude` _contains_ is [P13.02](02-default-exclude.md).
