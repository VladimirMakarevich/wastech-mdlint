# Accepted behaviors register

> Roadmap: [v2 Index](index.md) · Reference (not a precedence tier) · Established by [P12.06](P12-consistency/06-process-boundary-tests.md), deliverable 4.

The P11 remediation round fixed most of what the [post-P9 audit](audit-2026-07-25-post-p9.md) found, but some behaviors were deliberately **documented rather than changed**, and a few residuals were **recorded rather than closed**. Scattered across fifteen task files, those decisions are latent: a future reader hits the behavior, cannot tell whether it is intentional, and either "fixes" a deliberate tradeoff or files it again as a new finding.

This register is the one place they are stated. It is an **index, not a restatement**: each row points at where the behavior is already explained. Duplicating the explanation here would create a second copy to disagree with the first, so a row is a pointer plus the one-line reason it exists.

## How to use it

- **Adding a row.** When a task decides to accept a behavior instead of fixing it, add the row in the same change — the same "document where it is introduced" discipline the [glossary](glossary.md) and code comments follow.
- **User-facing vs maintainer-facing.** A behavior a _user_ can hit must have a home in `README.md` or `docs/guide/` — the register links it, it does not substitute for it. A residual only a maintainer can act on may cite its task file alone.
- **Removing a row.** When a later task actually fixes the behavior, delete the row rather than marking it stale.

## Accepted behaviors

Deliberate tradeoffs. Each is stated for users where they can hit it.

| Behavior | Why accepted | Stated for users in | Decided in |
| --- | --- | --- | --- |
| `columnMatches` accepts the `g`/`y` regex flags while they carry no meaning (each cell is a membership test, and match position is discarded between cells) | Keeps one uniform `flags` vocabulary across rules; rejecting them in the schema would have made a `"flags":"g"` config a validation error rather than a correct run | [TBL-004](../guide/rules/TBL-004.md), [custom rules](../guide/rules/custom.md) | [P11.05](P11-remediation/05-table-primitive-scope.md) |
| A target's own read-only mode no longer blocks a write: `--fix` rewrites a `0444` document and `init --on-existing overwrite` replaces a read-only config on Linux/macOS | Follows from replacing files by `rename()` (which needs directory, not target, permission). An `access(target, W_OK)` pre-check would be a TOCTOU race and would not hold on Windows; file mode was never the way to opt a document out of `--fix` — `exclude` is | [Output & exit codes](../guide/output.md) | [P11.09](P11-remediation/09-atomic-writes.md) |
| `wastech-mdlint ./docs` is `error: unknown command './docs'`, not a lint of `./docs` | An operand cannot be both a path and a misspelled command name. Only "no subcommand at all" was ever promised to lint the cwd, and every documented invocation names its subcommand | [CLI reference](../guide/cli.md) | [P11.10](P11-remediation/10-cli-exit-contract.md) |
| An `init` write that fails after the schema was committed leaves a `schema.json` behind (commit order is schema-first, config-last) | The config points at the schema, so schema-first keeps the old config and its still-accurate `$schema` valid if the schema rename fails. The reverse asymmetry is inherent without a journal, so it is reported in the partial-write summary rather than prevented | [Output & exit codes](../guide/output.md) (partial `init` summary) | [P11.09](P11-remediation/09-atomic-writes.md), [P11.14](P11-remediation/14-init-cli-lows.md) |
| The depth-agnostic `**/<name>/**` noise globs are the **lint-time** default `exclude` — applied on every run, not only after `init` — so hand-written docs under a nested directory literally named `build`/`out`/`vendor`/… are pruned | Root-anchored globs would have left a monorepo's `packages/*/dist` in the corpus — the exact thing the list prevents — and a second, narrower lint-time list would be the next thing to drift from what `init` writes. A negated entry naming the directory is the escape hatch | [Configuration](../guide/configuration.md#what-is-excluded-before-you-write-anything), [CLI reference](../guide/cli.md) | [P11.08](P11-remediation/08-init-exclude-anchoring.md), [P13.02](P13-correctness/02-default-exclude.md) |
| The `init` scan still prunes **every** dot-prefixed directory by shape, so `init` never proposes `.claude/skills/` or `.agents/rules/` as a doc cluster even though they are lintable by default | The prune is sound for _cluster inference_ — a name list cannot enumerate the tooling directories (`.github`, `.husky`, `.changeset`) that would pollute a proposal, which is audit L-7 — and [P14.03](P14-host-boundary/03-init-disclosure.md) deliberately left the scan's rationale alone while removing its lint-time counterpart. What makes it survivable is disclosure, not omission: the draft names the skipped directories and their Markdown count before you confirm. Widening what `init` infers is W-39 ([P16.05](P16-release-readiness/05-low-severity-cleanups.md)) | [CLI reference](../guide/cli.md#init) (the _Excluded from the scan_ block), [Configuration](../guide/configuration.md#what-is-excluded-before-you-write-anything) | [P14.03](P14-host-boundary/03-init-disclosure.md) |
| `compile`'s own output re-enters a zero-config corpus: its default outdir is `.claude/skills/wastech-mdlint/`, and a generated `SKILL.md` there is parsed by the next `lint` that runs without a config | The reverse of a benign side effect [P13.02](P13-correctness/02-default-exclude.md) noted, and the price of not excluding a directory for being hidden. Bounded: the zero-config ruleset is empty, so the cost is one parse and no findings; with a config, `include` governs and an `init`-written one never covers a dot-directory. Excluding the tool's own outdir by name would re-introduce a shape-based rule under a different name, and users retarget `--outdir` anyway | [Configuration](../guide/configuration.md#what-is-excluded-before-you-write-anything), [Compile](../guide/compile.md) (the output-path bullets) | [P14.03](P14-host-boundary/03-init-disclosure.md) |
| A user `exclude` **extends** the default rather than replacing it, so removing a default entry from your own `exclude` has no effect and `"exclude": []` is not an opt-out | Under _replace_, a config as ordinary as `"exclude": ["drafts/**"]` silently re-opens every `node_modules` tree — W-02 unfixed for exactly the users who engaged with the key, and silent in every direction (exit `0`, a plausible file count). Ordered evaluation already provides the escape hatch: `"!**/vendor/**"` un-excludes one tree, `"!**"` all of them | [Configuration](../guide/configuration.md#what-is-excluded-before-you-write-anything) | [P13.02](P13-correctness/02-default-exclude.md) |
| `STR-001` glob entries are satisfied only from the analyzed Markdown corpus, never expanded against the filesystem; and a glob cannot be pinned to the repository root | `Rule.check` is synchronous, so expanding globs would mean walking the tree from a sync call, and `include`/`exclude` already define what a run considers. Changing glob anchoring would silently break every existing entry rather than the narrow literal case | [STR-001](../guide/rules/STR-001.md) | [P11.12](P11-remediation/12-str001-reach.md) |
| Path containment is **lexical only**: a symlink that lives inside the analyzed root but points outside it is still probed | Resolving every candidate would add a syscall per reference on the hot path; the boundary check exists to stop the option being used as a host file-existence oracle, which lexical rejection already does | [STR-001](../guide/rules/STR-001.md) | [P11.02](P11-remediation/02-sec003-path-escape.md), [P11.12](P11-remediation/12-str001-reach.md) |
| `respectGitignore` matching is **pattern-only**: `.gitignore` files are read, but not `.git/info/exclude`, not a global `core.excludesFile`, and not git's index — so an already-**tracked** file that matches an ignore pattern is skipped even though `git` keeps it | Neither excludes file is claimed anywhere, and honoring them would widen the very contract [P13.03](P13-correctness/03-gitignore-precedence.md) exists to make honest. Consulting the index would mean shelling out to `git` or parsing it — neither fits a local-first matcher with no `git` dependency, and the answer would then depend on repository state rather than on the config | [Configuration](../guide/configuration.md#top-level-shape) | [P13.03](P13-correctness/03-gitignore-precedence.md) |
| `respectGitignore` patterns match **case-insensitively** on every platform, so a `README.md` pattern also skips `readme.md`, which `git` keeps on a case-sensitive filesystem | The `ignore` package's default, and pre-existing behavior that W-11 did not reach. Matching `git` would mean varying the corpus with the host filesystem's case sensitivity — the same class of host-dependence the pattern-only decision above rejects — while the current behavior is at least identical on Windows, macOS, and Linux | [Configuration](../guide/configuration.md#top-level-shape) | [P13.03](P13-correctness/03-gitignore-precedence.md) |
| Cycle detection is recursive, so one densely connected component of many thousands of documents can exhaust the call stack | Documented bound rather than a rewrite to an iterative traversal: many small components are fine at any corpus size, and no real corpus has reached it | [README limitations](../../README.md#limitations), [context graph limitations](../guide/context-graph.md#limitations) | [P12.05](P12-consistency/05-recursion-depth.md) |
| A negated `exclude` entry cannot re-include a file under a directory an earlier positive entry excludes — `["docs/private/**", "!docs/private/keepme.md"]` keeps `keepme.md` out | `shouldPruneDirectory` decides per directory before descending, so no file inside an excluded tree is ever offered to the file-level filter the negation acts on. Honoring it means walking every excluded tree — `node_modules` included — which is the cost `exclude` exists to avoid, on the same trees [P13.02](P13-correctness/02-default-exclude.md) is about to prune by default. `git` restricts its own patterns the same way, and negating the directory (or narrowing `include`) is the move in both | [Configuration](../guide/configuration.md#glob-semantics) | [P13.01](P13-correctness/01-glob-semantics.md) |
| `GRP-001` does not report a two-document mutual link at its default `minCycleLength` of 3, so a README indexing a sibling that links back is silent unless the option is lowered to `2` | Severity, not accuracy, was the defect: at `error` an ordinary documentation shape failed the build, and the likely response is disabling the rule, forfeiting the genuine 3- and 4-hop cycles it also finds. Keeping `error` and moving the floor preserves the rule's teeth for cycles that are actually tangled. The one dangerous two-node case — mutual eager `@import` — is reported by [`LLM-001`](../guide/rules/LLM-001.md) independently, so nothing loses its only home. `minCycleLength` also filters the _representative_ path per strongly connected component, not the component's shortest cycle | [GRP-001](../guide/rules/GRP-001.md) | [P13.04](P13-correctness/04-rule-option-defaults.md) |
| `SIZE-001`'s "at least one budget" rule is enforced only at config load, and is invisible in `packages/cli/schema.json` — an editor validating against the schema accepts `{"rule":"SIZE-001"}` that the linter then rejects | "At least one of three" has no JSON Schema-expressible shape in this generator: Zod refinements are dropped by `z.toJSONSchema`, and the alternatives are worse — marking one metric `required` would force a byte budget on a config that wants only a token one. A load-time diagnostic naming all three keys is the honest form; the previous behavior was no diagnostic at all | [SIZE-001](../guide/rules/SIZE-001.md) | [P13.04](P13-correctness/04-rule-option-defaults.md) |
| A user `GRP-002.entryPoints` **replaces** the default `["README.md", "CLAUDE.md", "AGENTS.md", "index.md"]` instead of extending it — the opposite of the top-level `exclude` row above | The asymmetry is deliberate. Dropping a default `exclude` re-opens `node_modules`, a blocker; dropping a default entry point at most re-reports a root the author chose not to list. Extend would make "do not exempt `README.md`" inexpressible, and an author writing `entryPoints` at all is naming the roots they know | [GRP-002](../guide/rules/GRP-002.md) | [P13.04](P13-correctness/04-rule-option-defaults.md) |
| "A Markdown file" is `.md` + `.mdx` and nothing else: neither is linted beyond `.md` by default, and a linked `.markdown` file is invisible to the coverage signal even though it exists on disk | One definition for the coverage signal, the `init` scan, and the default `include` is the whole point of W-09 — three sets meant coverage checked for an extension no default config could admit and missed the one `init` walks. `.markdown` appeared nowhere else in core and on no guide page, so recognizing it could never lead anywhere; adding `.mdx` to the linted corpus is a parser-implicating product decision this task holds out of scope, so the gap is a declared subset (`LINTED_MARKDOWN_EXTENSIONS ⊆ MARKDOWN_EXTENSIONS`) rather than three literals | [Context graph](../guide/context-graph.md#algorithms) (coverage caveat) | [P13.05](P13-correctness/05-reference-resolution.md) |
| An MCP argument the tool's own `inputSchema` rejects is refused before the handler runs, so it comes back as `isError: true` with **no `structuredContent`** and raw transport text carrying the JSON-RPC `-32602` number — outside the `{ code, message, hint }` contract entirely | Closing it means loosening every tool's advertised `inputSchema` to accept shapes it intends to reject, purely so the handler can reject them better — and that schema is what a host or model reads to _construct_ a valid call. The loosening is instead selective, aimed at the mistakes that need guiding advice ([P12.04](P12-consistency/04-mcp-custom-rules.md)'s `custom` entry union), which is the honest form of the trade. The exempt text does still name the offending field and its constraint, so a human or a model can act on it | [MCP server](../guide/mcp-server.md#error-contract) (the branch-on-`isError` note), [Glossary](glossary.md) **Error contract** | [P14.05](P14-host-boundary/05-mcp-error-contract.md) |
| An MCP `OPERATIONAL_ERROR` names only a path **inside** the tool's `cwd`: an errno whose path escapes it, or that reports no path at all (POSIX `EISDIR` from a `read`, `ENOSPC`), stays a sanitized `INTERNAL_ERROR` — where the CLI renders the `../` chain and keeps the original message | The bounded half of [W-21](remediation-backlog-2026-08-05.md)'s fix, and the reason it is safe to add a code that carries a path at all: "no payload names a host path _outside_ the analyzed directory" is the property `INTERNAL_ERROR`'s sanitization exists for — that directory itself is the one absolute path any payload carries, and only where [P14.01](P14-host-boundary/01-mcp-cwd-validation.md)'s `INVALID_INPUT` echoes back the `cwd` it could not use — and across Windows drives `path.relative` returns an absolute path outright. The CLI can afford the opposite choice because its output is a terminal, not a machine contract that a model may echo | [Output & exit codes](../guide/output.md#operational-failures-on-both-hosts) | [P14.05](P14-host-boundary/05-mcp-error-contract.md) |
| The generated `SKILL.md`'s `Role` column is coarse at scale: on the 139-document [large-corpus fixture](../../packages/core/test/support/large-corpus.ts) the five roles measure `hub` 73, `isolated` 46, `entry` 11, `bridge` 5, `leaf` 4 — two buckets holding 86%, reading in practice as "has edges" / "has no edges" (the field's 139-document corpus measured 83% the same way) | Neither lever works. Raising `hubMinInDegree`'s default leaves `isolated` untouched — that is a true signal about the corpus, not a bucketing artifact — and an absolute in-degree threshold cannot be scale-free (3 is right at 10 documents, noise at 1000), while making it relative would make one document's role depend on the rest of the corpus, breaking the local deterministic meaning the option advertises and silently changing every existing user's content hash for a cosmetic gain. Subdividing the vocabulary buys the same rounding one level down. What ships instead is the information the bucket discards: a `Refs (in/out)` column on every Architecture row, so a 3-reference hub is distinguishable from a 124-reference one | [Compile](../guide/compile.md#config) (the `hubMinInDegree` bullet) | [P15.01](P15-output-contracts/01-renderers-at-scale.md) |
| A cycle path past eight entries is elided in both human renderers, and the dropped middle is not readable from **any** CLI output: `graph --format json` serializes `{ nodes, edges, components, readingOrder, excluded, coverage }` and has no `cycles` field | The elision is what stops one large strongly connected component rendering as a ~3 500-character single line, which is the defect [P15.01](P15-output-contracts/01-renderers-at-scale.md) exists to fix. [P15.02](P15-output-contracts/02-graph-output-contract.md) then added `excluded` and left `cycles` out on purpose: W-23 asked _why a node is excluded_, which is per-node, and a corpus-wide cycle list is a different signal that would widen that task's deliverable. The loss is bounded: that JSON's edge list is complete, so the cycle is re-derivable, and the MCP `context-graph` tool's `format: "raw"` returns `cycles` in full | [Context graph](../guide/context-graph.md#graph) (the `human`-format note) | [P15.01](P15-output-contracts/01-renderers-at-scale.md), key list updated by [P15.02](P15-output-contracts/02-graph-output-contract.md) |
| The graph's excluded set names the nodes a topological sort could not place, but not **why** each one is unplaceable — in a cycle, or reachable only through one | Both formats now carry the set, which is the gap that mattered (a reading order 43 of 139 nodes short, unexplained). The reason is a wider change than a field: it means running SCC over the excluded subgraph inside `topologicalSort`, whose `{ order, excluded }` result also feeds `ImpactClassification`, both human renderers, and the generated skill's `Reading Order` block — four surfaces, so it is its own task rather than a rider on this one. The two reasons are stated as a pair in the guide, so a reader knows the closed set of causes even without the per-node attribution | [Context graph](../guide/context-graph.md#graph) (the excluded-set note) | [P15.02](P15-output-contracts/02-graph-output-contract.md) |
| `--format`'s vocabulary differs across sibling commands: `graph` takes `human\|json\|mermaid\|dot` (default `human`) and rejects `text`, while `lint`/`slice`/`impact` take `text\|json` (default `text`) and reject `human` | Accepting both words on both commands would make `graph --format text` ambiguous, since `mermaid` and `dot` are plain text too — on that command the distinguishing property is the audience, not the encoding, which is what `human` says and `text` would not. Renaming either side would break existing invocations for a vocabulary gain. It stays **loud**: a wrong word exits `2` naming the valid choices for the command actually run, and the guide now states the intent rather than only the mechanics | [CLI reference](../guide/cli.md#graph) (the note under the flag table) | [P15.02](P15-output-contracts/02-graph-output-contract.md) |
| The generated `SKILL.md`'s per-direction reference bound counts **edges, not distinct documents**, so one referencing document can consume several of the ten slots and appear more than once in the list (a plain link and an anchored one to the same file are two edges) | The multiplicity is upstream of the renderer: the graph deliberately keeps one edge per source construct and dedup-with-count is still a G7 backlog item, so deduping inside `synthesize.ts` would make the artifact's `- from (124, showing 10):` count and its bullets speak about two different units — the disclosure would then have to explain both. The bound's purpose is a fixed ceiling on the block's size, which per-edge slots deliver exactly; a reader who needs the referencing _files_ has `impact <file>`, which reports them | [Compile](../guide/compile.md#the-dependency-section-is-bounded-and-says-so) (the per-direction bullet) | [P15.01](P15-output-contracts/01-renderers-at-scale.md) |
| Every finding's `helpUri` points at `blob/main`, so a pinned older install links to the **current** documentation for its rule rather than to the docs that shipped with it | Nothing at runtime knows which version is installed, and the guide pages are in no published tarball, so there is no version-matched page to link even if it did. `main` at least always resolves; a tag baked in at build time would be a second version string to keep in step with `package.json`, and a stale tag would 404 rather than drift. The pages describe rule semantics, which move rarely, and the finding also carries `ruleId` for a reader who wants their own copy | [Output & exit codes](../guide/output.md#message-keys) (the `helpUri` row) | [P15.03](P15-output-contracts/03-lint-output-contract.md) |
| The compiled `SKILL.md`'s `Context Budget` block reports token numbers — a corpus total and each entrypoint's breach — with **no** calibration, while the lint findings quoting the same estimate now carry it in their own message | The artifact is loaded into an agent's context whole, so a sentence repeated per entrypoint spends the context that block exists to protect, and appending it would move the bytes and content hash of every committed `SKILL.md` — an artifact-wide re-render for a disclosure a prose page can carry once. The block's phrasing deliberately mirrors LLM-001's report; that parity is now approximate rather than exact, and the guide states which surface says what | [Compile](../guide/compile.md#the-context-budget-numbers-are-estimates), [Concepts](../guide/concepts.md#token-estimation) | [P15.03](P15-output-contracts/03-lint-output-contract.md) |
| Config validation does not aggregate across its two stages: a file with both a shape error and a rule-options error reports only the shape error, so clearing it takes two runs | Stage 2 resolves rules from stage 1's _parsed_ output, so running it on a shape the schema rejected would feed `resolveRule`/`resolveCustomRule` inputs they are not typed for — the crash class audit M-3 closed. Each stage does report all of _its own_ issues at once, so the bound is two passes, not one error per run | [Configuration](../guide/configuration.md#validation--errors) | [P13.06](P13-correctness/06-config-diagnostics.md) |

## Recorded residuals

Known, narrow, and not fixed. These are maintainer-facing: no user action closes them, so they cite their task file rather than a guide page. Listed so a later sweep re-finds them as _known_ rather than filing them again.

| Residual | Why not fixed | Recorded in |
| --- | --- | --- |
| A `STR-001` literal whose filename is itself glob syntax (`LICENSE(1)`, `docs[x]/a.md`) is classified as a glob and routed to the corpus branch, so a non-Markdown file at that path still reports missing | Unchanged pre-existing behavior (every entry took the corpus branch before P11.12), needs a filename that is glob syntax, and the fix would be an escaping vocabulary the option does not have | [P11.12](P11-remediation/12-str001-reach.md) |
| `candidateEscapesRoot` does not repeat `resolvesOutsideRoot`'s resolve-and-compare step, so a candidate normalizing to a bare drive-relative remainder (`c:secret.md`) on a different drive than the root is not caught at the three `existsSync` call sites | Reaching it needs an attacker-authored document, a Windows host, and a `..`-cancellation leaving a bare drive letter with no root separator — materially narrower gain than the fix that shipped. `SEC-003`'s `template` is fully covered | [P11.02](P11-remediation/02-sec003-path-escape.md) |
| A gitignored workspace package is still detected by `init` and still produces a (empty) scan scope; the gitignore layers are threaded only through the Markdown walk, not through workspace-package detection | The asymmetry is invisible in the draft — an empty scope proposes nothing — and workspace detection reads `package.json` files, which `.gitignore` rarely speaks about deliberately | [P11.14](P11-remediation/14-init-cli-lows.md) |
| The `init` draft a user confirms does not name the project-local `schema.json` the `npx` path writes; only the after-the-fact write summary does | A gap against the warn-before-confirming discipline, explicitly **deferred** rather than accepted — the one row here a future task should close rather than keep | [P11.14](P11-remediation/14-init-cli-lows.md) |
| `init`'s `GRP-001` rationale can cite a sampled two-document cycle beside an entry that, at `minCycleLength`'s default of 3, will not report it — the proposed config is valid, but its own comment describes a finding the run will not produce | The inference heuristic and the rule's finding floor moved in different changes; [P13.04](P13-correctness/04-rule-option-defaults.md) is scoped to the config-authoring path and touching inference belongs with the rest of `init`'s widening (W-39/[P16.05](P16-release-readiness/05-low-severity-cleanups.md)). Narrow because it needs a sampled back-link, and the config it writes is correct either way | [P13.04](P13-correctness/04-rule-option-defaults.md) |
| The shared `assertBuilt()` still decides "built" from **modification times**, not from build state, so a source file whose timestamp moved without its content changing still fails the check that `npm run build` cannot clear | The harmful half was the _message_, which named the command that had just exited `0`; it now names `npx tsc -b --force` as the fallback and says why (verified in this tree: a forward `utimes` on `packages/cli/src/index.ts` left `tsc -b` a no-op and `tsc -b --force` re-emitted). Comparing real build state means re-deriving `tsc -b`'s own `.tsbuildinfo` bookkeeping inside a test helper — a second implementation of the thing under verification, and one that would go wrong silently where a timestamp goes wrong loudly | [P16.01](P16-release-readiness/01-test-debt.md) |
| The glossary's **`lintFiles`** entry no longer claims the pipeline entry point is "intentionally **synchronous** (`globSync` + `readFileSync`)", but the enforced ADR it cited as its authority — [core-hosts-the-pipeline](decisions/core-hosts-the-pipeline.md) — still carries that sentence verbatim, so the two documents disagree and the glossary is the side that matches the code | Correcting an `Accepted (enforced)` decision, and settling the two further glossary sites that restate its framing (**LSP server**, **Async rules**), is W-41 — owned by [P17.03](P17-plan-of-record/03-adr-and-dependency-register.md) and ranked first in its phase because a tier-3 document is what a reviewer cites to block a change. [P16.01](P16-release-readiness/01-test-debt.md) corrected the glossary entry because its own change made that entry wrong in a second way (the step order moved to `lintCorpus`); rewriting the ADR from a test-debt task would take the decision out of the task that owns it. Named in the glossary entry itself so a reader meets the divergence rather than one half of it | [P16.01](P16-release-readiness/01-test-debt.md), closed by [P17.03](P17-plan-of-record/03-adr-and-dependency-register.md) |
| A `siteRouter` with no known `preset` (a bare `{}` included) returns the stripped URL **un-normalized**, so `/gen/../generated/x.md` yields the candidate `gen/../generated/x.md` where the no-router branch yields `generated/x.md`. `existsSync` still resolves it, but an `exclude` glob written against the normalized path does not match it | Narrow (needs a `..` segment inside a root-relative target _and_ a router set) and pre-existing — outside W-08, whose defect was `exclude` not being consulted at all. `resolveTargetCandidates` is read by three subsystems, so normalizing the unknown-preset branch is a shared-behavior change that wants its own task rather than a rider on a shared branch | [P13.05](P13-correctness/05-reference-resolution.md) |
| The absolute-path fallback for an out-of-`cwd` target is scoped to `compile`'s write summary, so argument diagnostics (`resolveDirectoryArgument`) and the errno backstop (`formatOperationalError`) still render a path above `cwd` as a `../` chain | Widening it would reverse [P11.10](P11-remediation/10-cli-exit-contract.md)'s deliberate promise that a `[path]` passed absolutely is _reported_ relatively, and would loosen the "an error never prints an absolute host path" property those surfaces are pinned on. A written file is the opposite case — the user has to open it. The asymmetry is narrow (an argument outside `cwd` at all) and nothing depends on the current rendering, so widening later stays cheap | [P14.02](P14-host-boundary/02-cli-exit-codes.md) |
| `init` recursively walks every pruned **hidden** tree to size it for the disclosure, so a hidden cache `DEFAULT_NOISE_DIR_NAMES` does not name (`.tox`, `.gradle`, `.mypy_cache`, `.pytest_cache`, `.terraform`, `.turbo`, `.nx`, …) gets a `readdir` plus a `.gitignore` probe per directory for a number the user is only told about. Being gitignored does not stop it either — `"hidden"` is classified before the gitignore test, which also decides how such a tree is _disclosed_: it appears on the hidden line and never on the gitignored one, so whatever Markdown the ignore patterns do not themselves match is counted and offered an `include` suggestion that the `respectGitignore: true` a fresh `init` writes will not honor | The bound is that name list, so it is incomplete by construction: `.venv`/`.yarn` cover the two trees the field test actually hit, and further names are a one-line addition when one shows up. The alternative — bounding by _shape_ rather than by name — is exactly what [P14.03](P14-host-boundary/03-init-disclosure.md) removed from the lint-time default, and here it would make the disclosed count silently wrong for the dot-directories W-14 exists to surface: a `.claude/skills/` reported as `0 Markdown files` is worse than a slow scan. Bounded to `init`, a one-off command, and never on the lint path | [P14.03](P14-host-boundary/03-init-disclosure.md) |
| `endLine` is declared on `LintMessage`, mirrored in the MCP wire schema, and set by **no** shipped rule, so it never appears in output — R3's one remaining unrealized structured field | Populating it means every primitive that currently reports one line deciding where its finding _ends_, which is a per-rule span semantics question (does a bad table cell span the row, the cell, the table?) rather than a field to fill in. [P15.03](P15-output-contracts/03-lint-output-contract.md) settled R6's two vacuous metadata fields by taking a decision on each (populate `docsUrl`, drop `messages`); the same decision here is either 24 span judgements or removing a field, and neither is a rider on a task about documenting output. Documented as declared-but-unset in the [message-key table](../guide/output.md#message-keys) instead, so no consumer waits for it | [P15.03](P15-output-contracts/03-lint-output-contract.md) |
| The `--no-install` flag on the `npx` smoke check in `packages/cli/test/bin.e2e.test.ts` is load-bearing nowhere: it is an npx-v6 spelling the pinned npm parses as an unknown config. The check's `cwd` is the whole no-network guarantee | Harmless where it sits, and removing it is a test-hygiene cleanup rather than a behavior fix | [P11.01](P11-remediation/01-cli-bin-noop.md) |

## See also

- [Glossary](glossary.md) — the canonical vocabulary, including **Accepted-behaviors register** and **Process-boundary guard**.
- [Process-boundary guards](../../.agents/rules/testing.md) — the paired checklist of guards the product must keep, enforced by `packages/core/test/boundary-guards.test.ts`.
- [Post-P9 audit](audit-2026-07-25-post-p9.md) — the findings these decisions answer.
