# Phase P18 — Follow-up burn-down

> Roadmap: [v2 Index](../index.md) · Phase **P18** · Size **M** · Status **In progress** · Depends on [P17](../P17-plan-of-record/index.md) (the plan of record now describes what shipped).
>
> **Goal:** close the small defects the P13–P17 rounds recorded rather than fixed, and put the ones verification already closed on the record so nobody re-opens them. Sourced from the orchestrator's accumulated follow-up file, triaged on 2026-08-07.

## Where these items come from

Twenty finished orchestrator tasks (`p13-01` … `p17-04`) each appended their unfixed findings to `.worc/follow-ups.md` — a gitignored, append-only queue with no ids and no status, where deleting an entry is how it is closed. That file held **95 entries**. Triage on 2026-08-07 reduced them to **75 distinct items** and checked each against the tree:

|  | Count | Disposition |
| --- | --- | --- |
| Entries in the source file | 95 | Each task recorded its review-accepted findings and its mid-run observations separately, so most items were listed twice — once as a one-line title, once as the reviewer's full paragraph |
| Distinct items | 75 | The unit this phase tracks, as `FU-01` … `FU-75` |
| Already closed | 35 | Fixed by a later task in the same series, answered by verification, or already decided and registered as deliberate — recorded below, not re-tracked |
| Live | 40 | Grouped into the six burn-down tasks and three spun-out tasks in the table below |

**Why the tasks are grouped by surface rather than by the task that found the item.** `glossary.md` was named by nine entries from six different rounds, `compile/synthesize.ts` by five, `config-reference.md` by four. Cutting the work by originating task would open the same file in six tasks and pay for six merges of one file; cutting it by surface pays for one.

**One cause produced most of the stream.** Fifty-five of the 75 items are a documentation claim that outran the code — a count stated wrong, an equivalence stated too strongly, a comment naming an expression that no longer exists. They were generated by the very tasks that wrote those documents, which is why so many are already closed: the next round's docs pass swept them without knowing they were recorded. [P18.09](09-doc-citation-guard.md) is the only item in this phase that addresses the cause rather than an instance.

## Tasks

| # | Task | Items | Sev | Size | Depends on |
| --- | --- | --- | --- | --- | --- |
| [P18.01](01-compile-renderers.md) | Compile renderers: cycles block and token-note parity | FU-03, FU-04 | Medium | S | — |
| [P18.02](02-code-fixes.md) | Twelve localized fixes across core, the CLI and the MCP host | FU-05 … FU-13, FU-15, FU-16, FU-68 | Medium | M | — |
| [P18.03](03-core-test-pins.md) | Core behavior nothing currently proves | FU-18, FU-19, FU-20, FU-22 | Medium | M | P18.02 |
| [P18.04](04-host-test-pins.md) | Host and packaging pins, and three stale test names | FU-24 … FU-33, FU-75 | Medium | M | P18.02 |
| [P18.05](05-doc-claims.md) | Three surviving documentation over-claims | FU-37, FU-45, FU-48 | Medium | S | P18.01 |
| [P18.06](06-plan-residue.md) | Plan-of-record residue P17 left behind | FU-54, FU-56, FU-61, FU-62 | Low | S | — |
| [P18.07](07-dependency-register-audit.md) | Trace the remaining dependency-register entries | FU-71 | Medium | M | — |
| [P18.08](08-excluded-reason-attribution.md) | Per-node reason for the graph's excluded set | FU-72 | Low | M–L | — |
| [P18.09](09-doc-citation-guard.md) | A guard against docs citing APIs that do not exist | FU-73 | Medium | M | P18.02 |

**Sequence.** [P18.01](01-compile-renderers.md) and [P18.02](02-code-fixes.md) first, because the test pins in [P18.03](03-core-test-pins.md)/[P18.04](04-host-test-pins.md) pin the corrected behavior and [P18.05](05-doc-claims.md) describes it. [P18.06](06-plan-residue.md) touches only plan documents and can run at any point. The three spun-out tasks are independent of the burn-down; [P18.09](09-doc-citation-guard.md) comes last of them so it runs against a tree whose citations are already correct.

## Progress

| Task | Status | Items closed |
| --- | --- | --- |
| [P18.01](01-compile-renderers.md) | **Done** | FU-03, FU-04 (FU-02 closed as an existing decision) |
| [P18.02](02-code-fixes.md) | **Done** | FU-05 … FU-13, FU-15, FU-16, FU-68, plus FU-23 (same file as FU-12) |
| [P18.03](03-core-test-pins.md) | **Done** | FU-18, FU-19 (as a pin of the _opposite_ default), FU-20, FU-22 |
| [P18.04](04-host-test-pins.md) | **Done** | FU-24 … FU-33, FU-75 |
| [P18.05](05-doc-claims.md) | **Done** | FU-45, FU-48 (FU-37 closed on arrival) |
| [P18.06](06-plan-residue.md) | **Done** | FU-54, FU-56, FU-61, FU-62 |
| [P18.07](07-dependency-register-audit.md) … [P18.09](09-doc-citation-guard.md) | Not started | — |

**37 of the 40 live items are closed, 3 remain — the six burn-down tasks are done, and what is left is the three spun-out ones.** Four of them turned out not to be defects at all. FU-02 and FU-21 were behaviors already decided and registered, which the triage read as live because it checked the source and not the register. FU-37 was already fixed, by the docs pass of a round that never saw the follow-up — the same sweep that had closed 35 items before triage, still running. FU-19 was the same lesson inverted: it asked for a test pinning `**/.*/**` in the lint-time default exclude, a glob [P14.03](../P14-host-boundary/03-init-disclosure.md) had removed a phase after the item was recorded, so writing the step as prescribed would have asserted the opposite of what ships — it was closed by pinning the surviving half, the missing end-to-end coverage, against the _current_ default. So: **an item that still reproduces may be a documented decision, and an item that no longer reproduces may still name a real gap.** The register and the owning task's implementation notes are part of verifying one, not just the tree, and the follow-up file carries no dates or status to tell an item recorded before a decision from one recorded after it.

## Closed on arrival — do not re-open

These items were live when they were recorded and are not now. Kept here because the source file is gitignored and its entries are deleted on close, so this table is the only surviving record that they were considered.

| Item | Subject | Why it is closed |
| --- | --- | --- |
| FU-01 | The docs corpus might carry a case-mismatched link target that `REF-001`'s `existsSync` fallback hides on APFS and that fails only on the `ubuntu-latest` leg — the "gate red on arrival" risk [P17.02](../P17-plan-of-record/02-self-linting-config.md) was warned about | **Verified clean on 2026-08-07.** Every relative link target in the 221-file corpus was resolved against exact-case `git ls-files` output rather than `existsSync`: 3031 targets outside code fences, **zero** case mismatches and zero unresolved. The macOS-only evidence in P17.02's notes now has a case-sensitive counterpart |
| FU-02 | `renderEdgeBullets` caps _edges_, not distinct referencing documents, so ten slots can show four documents and two byte-identical bullets | **Already decided, not unfixed.** The follow-up offered dedupe _or_ stating the unit; the second was taken at all three sites it named — [P15.01](../P15-output-contracts/01-renderers-at-scale.md)'s notes, a register row, and `compile.md`'s "the unit is **edges, not distinct documents**" with `impact <file>` named as the per-file view. Deduping would reverse an accepted behavior and depends on the `G7` dedup-with-count item. See [P18.01](01-compile-renderers.md) |
| FU-21 | `formatScanExclusions` asserts unconditionally that dot-directories are never proposed as doc clusters, which looked fixture-shaped | The assertion is sound because the scan **prunes every dot-prefixed directory by shape** — a registered, deliberate behavior, so a proposed dot-directory cluster is a state the code cannot reach and the requested fixture would test an impossible one. The "multiple hidden roots" half the follow-up also asked for is already covered by the existing fixture |
| FU-37 | `custom.md`'s "a malformed `custom` entry is a config error" bullet put a config-load sentence and an MCP-argument-validation sentence side by side, reading as a contradiction unless the reader noticed the different subjects | Already carries the prescribed fix, found when [P18.05](05-doc-claims.md) opened the file: the MCP half announces itself, the follow-on sentence opens "In that tool", and it links the error contract. Fixed by a later round's docs pass without knowing it was recorded |
| FU-14 | `operational-error.ts` is a third copy of the same two-field errno guard | Deliberate host-local judgment, accepted at [P14.05](../P14-host-boundary/05-mcp-error-contract.md). The recorded trigger is a **fourth** copy appearing; there are still three |
| FU-17 | `schema.ts`'s why-comment said "P13.04 added two" where the change added four defaulted keys | The comment no longer states a count |
| FU-34 | `lintCorpus`/`LintCorpusInput` on core's barrel looked like scope creep | Resolved the other way: `lintCorpus` is now deliberately **unexported**, and the glossary says so |
| FU-35, FU-36 | `config-reference.md` claimed every config mistake renders as `Invalid config at …` (a JSONC syntax error does not), and used a `maxToken` option no rule has | Now reads "Anything the schema rejects", and the example is `Unrecognized key: "token"` against `SIZE-001`'s real `tokens` |
| FU-38, FU-39, FU-40 | `configuration.md`'s glob section overclaimed universality (`STR-001`'s `files` is an exception), called `.gitignore`'s anchoring "opposite" when it is the same, and named three excluded ignore sources without naming case-insensitive matching | All three corrected in place; the section now carries the `STR-001` carve-out, the real `.gitignore` difference, and "Two caveats remain" |
| FU-41 | `getting-started.md` still stated the pre-[P13.02](../P13-correctness/02-default-exclude.md) zero-config corpus | Corrected inline, matching `configuration.md` |
| FU-42 | The register row for `compile`'s output re-entering a zero-config corpus had no user-facing home | `compile.md` now carries the bullet the row points at |
| FU-43, FU-44 | `README.md` omitted `lint:docs` from the local gate list and quoted `init`'s advice with the narrower `*.md` tail | Both present and correct |
| FU-46, FU-47 | `cli.md` and the glossary asserted argument paths are always relative (false across Windows drives) and that a missing absolute `--config` is named as typed | Both now carry the drive carve-out and the relativized-absolute rule |
| FU-49 | `output.md` marked `helpUri` always-present against a declared-optional type | The row now states it stays _declared_ optional and why |
| FU-50, FU-51, FU-52, FU-53 | `REF-001`'s bare-`{}` equivalence, `SIZE-001`'s load-time-only budget check, `context-graph.md`'s unqualified `GRP-001` bullets, and `P11.13`'s superseded "no options" claim | All four pages carry the qualifier or the supersession pointer |
| FU-55, FU-57, FU-58, FU-59, FU-60 | `completion-surface.md`'s "three sites" exhaustiveness claim, `P-release`'s bundled I6 row, `index.md`'s reference line, the dropped residuals link in `P16.01`, and the undercounted negation cases in `P13.01` | Closed by [P17.04](../P17-plan-of-record/04-completion-surface.md) and [P17.06](../P17-plan-of-record/06-register-and-roadmap.md) |
| FU-63 | Whether the 23-file diff recorded for [P17.01](../P17-plan-of-record/01-dead-links.md) included sibling-task work | A pre-merge attribution question about work already committed on the shared branch. No defect either way, and nothing to fix |
| FU-64, FU-65, FU-66, FU-67, FU-69, FU-70 | The glossary's `Rule` entry missing `docsUrl`, the did-you-mean property count disagreeing with requirements, the LSP entry's blanket `async` claim, the overstated graph-JSON parity, the `orphans-and-cycles.md` comment missing `excluded`, and the `lintFiles` synchronicity contradiction with the enforced ADR | All six corrected, the last by [P17.03](../P17-plan-of-record/03-adr-and-dependency-register.md) |
| FU-74 | `chmodSync(packages/cli/dist/index.js)` fails with `EPERM` in a sandboxed review environment | An environment note recorded twice, not a defect. The suite passes where `dist` is chmod-able |

## How this phase closes

A burn-down task is done when its items are fixed **and** their entries are deleted from `.worc/follow-ups.md` — deletion is the close mechanism for that file, and this phase is what survives it. Per [completion surface](../completion-surface.md), the author ticks the exit criteria; nothing derives them.

**Reference:** [Glossary](../glossary.md) · [Accepted behaviors](../accepted-behaviors.md) · [Completion surface](../completion-surface.md)
