Enrich the task into a complete, unambiguous specification. Document any assumptions. Do not edit code. Return the typed structured result required by the output schema. Set human_input only when a material ambiguity cannot be resolved safely from repository evidence. If a human_input context file is present, apply that answer and do not repeat the same question.

Project context — this repository is `wastech-mdlint`, a TypeScript/Node (ESM) CLI, and in the v2 target an MCP server, that audits Markdown context quality (size, links, dependency graph, LLM entrypoint budgets, structure). It is mid-rebuild from the current single package to v2, so resolve ambiguity from the sources of truth before asking a human: `AGENTS.md` and its precedence rules, and `docs/mdlint_v2/` — the roadmap (`index.md`), the locked `requirements/`, the `decisions/` (D1–D7), and the numbered phase task file for this task's phase. Honor the greenfield stance (no backward-compatibility or migration layers) and keep the enriched spec inside the scope of the task's phase — do not smuggle in later-phase work.
