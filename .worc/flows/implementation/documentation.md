Update the project's documentation so it reflects the change just implemented.{?plan_path} The plan is at {plan_path}.{/plan_path}{?diff_path} The diff showing exactly what changed is at {diff_path}.{/diff_path} Review what changed, then bring the affected docs in line — READMEs, the docs/ tree, configuration/usage references, changelog, public API docs, and any in-repo guide the change touches. Follow the project's existing documentation conventions and formatting; if the project ships a docs formatter or linter, run it. Make a focused, minimal change — do not edit code, tests, or behavior, and do not restructure docs beyond what this change requires. If nothing in the documentation needs updating, make no edits.

Document the **why, not the what**. The code and the diff already show _what_ changed; docs earn their keep by capturing what those cannot — the intent behind the change, the rationale for the approach, the constraints and trade-offs, and how a reader should use it. Do not narrate the change line by line or restate the code in prose. When a decision has a non-obvious reason, record the reason; when behavior is deliberately limited, say why it is limited rather than only that it is.

When the change adds, renames, or retires a **load-bearing term** — a public type, config key, CLI flag, API endpoint, or other name a reader will look up — keep any glossary or terminology reference the project maintains in sync with it.

**Stay strictly within documentation.** You are read-only to the code and to the build state — your only job is editing docs files, plus the one format gate below. Do **not** install dependencies (`npm install`/`npm ci`/`pnpm`/`yarn add`, `pip install`, etc.), run builds, tests, or any other workspace script (`workspace:*`, build/compile/bundle steps), start servers, reach the network, or run throwaway experiments to "check" something. If you need to know what the code does, read it and the diff — never execute or mutate the workspace to find out. If a doc claim genuinely cannot be settled without building or installing, write it conservatively (or note the uncertainty) rather than mutating the environment; that is a follow-up for a code step, not the documentation step.

**Leave the Markdown format gate green before you finish — this is the last step that touches the tree.** Your edits are committed by this run and no check runs after you, so a doc-only edit can turn the repository's gate red exactly as a code change can, and this project has already had that reach a branch. Run this repository's own check — `npm run format`, which is `prettier --check .` — and repair what it flags with a targeted `npx prettier --write <path> [<path>…]`, never a repo-wide rewrite. Scope that to the files this task touched: if the check flags a file no step of this task edited, leave it alone and name it in your final message — a pre-existing violation elsewhere is not yours to rewrite. Then re-run the check and confirm your own files are clean. This is exempt from the "no workspace scripts" rule above and is the only exemption: `prettier --check` mutates nothing, needs no network, and is already installed in this clone, so it needs no install of any kind. Remember that `.prettierrc.json` sets `proseWrap: "never"` — one paragraph is one line and the formatter reflows it for you, so never hand-wrap prose to a column.
