Update the project's documentation so it reflects the change just implemented.{?plan_path} The plan is at {plan_path}.{/plan_path}{?diff_path} The diff showing exactly what changed is at {diff_path}.{/diff_path} Review what changed, then bring the affected docs in line — READMEs, the docs/ tree, configuration/usage references, changelog, public API docs, and any in-repo guide the change touches. Follow the project's existing documentation conventions and formatting; if the project ships a docs formatter or linter, run it. Make a focused, minimal change — do not edit code, tests, or behavior, and do not restructure docs beyond what this change requires. If nothing in the documentation needs updating, make no edits.

Document the **why, not the what**. The code and the diff already show *what* changed; docs earn their keep by capturing what those cannot — the intent behind the change, the rationale for the approach, the constraints and trade-offs, and how a reader should use it. Do not narrate the change line by line or restate the code in prose. When a decision has a non-obvious reason, record the reason; when behavior is deliberately limited, say why it is limited rather than only that it is.

For `wastech-mdlint` specifically, the docs most often affected are `README.md` (the rules and CLI tables, config examples) and the relevant file under `docs/mdlint_v2/` when the change touches a requirement or advances a phase. When the change adds, renames, or retires a **load-bearing term** — a public type, config key, CLI flag, MCP tool, rule ID, edge type, node role, or assertion primitive — update `docs/mdlint_v2/glossary.md` in the same pass: add, rename, or retire the entry, and correct any shipped-vs-planned marker the change affects. The glossary is a lookup reference, not a precedence tier, so keep it consistent with the phase/requirement/decision docs rather than letting it contradict them. When the change **completes a phase**, update that phase's task file: set **Status → Done**, check its exit-criteria boxes, and add an **Implementation notes** section for the non-obvious decisions — touch only the phase this task belongs to, and do not flip sibling phases' files. State genuine limitations plainly — the roadmap deliberately values honesty about deferred or partial behavior over hiding it. Run the project's Prettier if it is configured (`npm run format`), and keep paths in docs repository-relative and POSIX-style.

**Stay strictly within documentation.** You are read-only to the code and to the build state — your only job is editing docs files (and running the docs formatter above). Do **not** install dependencies (`npm install`/`npm ci`/`pnpm`/`yarn add`), run builds or workspace scripts (`workspace:*`, `npm run build`, compile/bundle steps), start servers, reach the network, or run throwaway experiments to "check" something. If you need to know what the code does, read it and the diff — never execute or mutate the workspace to find out. If a doc claim genuinely cannot be settled without building or installing, write it conservatively (or note the uncertainty) rather than mutating the environment; that is a follow-up for a code step, not the documentation step. The docs formatter (`npm run format`) is the one sanctioned command.
